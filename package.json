{
  "name": "horizon-71",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "import": "python scripts/import.py",
    "test:e2e": "playwright test",
    "check:live": "node scripts/browser-check.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
